# NOTE: on Python 3.13, plain "dlib" fails to build from source on Windows.
# This project uses dlib-bin (prebuilt wheel) instead - do not switch back to
# plain "dlib" or face_recognition install will break again.
#
# As of the ML model integration, vision/face_engine.py no longer uses
# face_recognition/dlib (identity now runs on the trained YuNet+SFace ONNX
# model via OpenCV, which is already covered by opencv-python below).
# These three packages are left installed/listed in case anything else in
# your environment still depends on them; they are safe to remove later.
setuptools<81
dlib-bin
face-recognition>=1.3.0
face_recognition_models

python-telegram-bot>=22,<23
pygame>=2.6
opencv-python>=4.10
ultralytics>=8.3
pyserial>=3.5
numpy>=1.26
fastapi>=0.115
uvicorn>=0.30
pyttsx3>=2.98
pyaudio>=0.2.14
python-dotenv>=1.0
